<?xml version="1.0" encoding="UTF-8" ?>
<odoo>


    <record id="view_project" model="ir.ui.view">
        <field name="name">project.project.tree.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.view_project" />
        <field name="arch" type="xml">
            <field name="stage_id" position="after">
                <field name="restrict_selectable_external_users" optional="hide" />
                <field
                    name="selectable_external_user_ids"
                    widget="many2many_tags"
                    optional="hide"
                />
            </field>
        </field>
    </record>



     <record id="edit_project" model="ir.ui.view">
        <field name="name">project.project.form.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project" />
        <field name="arch" type="xml">
            <field name="privacy_visibility" position="after">
                <field name="restrict_selectable_external_users" />
                <field
                    name="selectable_external_user_ids"
                    invisible="not restrict_selectable_external_users"
                    options="{'no_create': True}"
                    widget="many2many_tags"
                />
            </field>
        </field>
    </record>
</odoo>
